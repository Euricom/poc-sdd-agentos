/**
 * UI-specific TypeScript types for the Agent Hub application
 *
 * Note: Agent type is imported from @/lib/mock-data and serves as the
 * single source of truth for agent data structure.
 */

/**
 * Avatar size variants
 */
export type AvatarSize = 'sm' | 'md' | 'lg';
